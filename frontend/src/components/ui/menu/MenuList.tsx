import styled from "styled-components";

export const MenuList = styled.ul`
  position: relative;
  margin: 0;
  padding: 8px 0;
  list-style: none;
  && {
    padding-left: 0;
  }
`;

MenuList.displayName = "MenuList";
